{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pay</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pay.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ pay.nom }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ pay.code }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_pays_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_pays_edit', { 'id': pay.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
