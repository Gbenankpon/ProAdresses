{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pays list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Code</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pay in pays %}
            <tr>
                <td><a href="{{ path('admin_pays_show', { 'id': pay.id }) }}">{{ pay.id }}</a></td>
                <td>{{ pay.nom }}</td>
                <td>{{ pay.code }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_pays_show', { 'id': pay.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_pays_edit', { 'id': pay.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_pays_new') }}">Create a new pay</a>
        </li>
    </ul>
{% endblock %}
