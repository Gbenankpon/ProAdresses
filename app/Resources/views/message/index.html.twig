{% extends 'base.html.twig' %}

{% block body %}
    <h1>Messages list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Prenom</th>
                <th>Nom</th>
                <th>Courriel</th>
                <th>Message</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for message in messages %}
            <tr>
                <td><a href="{{ path('admin_message_show', { 'id': message.id }) }}">{{ message.id }}</a></td>
                <td>{% if message.date %}{{ message.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ message.prenom }}</td>
                <td>{{ message.nom }}</td>
                <td>{{ message.courriel }}</td>
                <td>{{ message.message }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_message_show', { 'id': message.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
